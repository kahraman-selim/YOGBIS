@model IEnumerable<YOGBIS.Common.VModels.AdaylarVM>

<div class="table-responsive">
    <table class="table table-bordered table-hover table-striped display nowrap" id="myTable" width="100%" cellspacing="0">
        <thead>
            <tr>
                <th class="text-center">SN</th>
                <th class="text-center">TC</th>
                <th class="text-center">Ad</th>
                <th class="text-center">Soyad</th>
                <th class="text-center">Yýl</th>
            </tr>
        </thead>
        <tbody>
            @{
                var sirano = 0;
            }
            @if (Model != null)
            {
                @foreach (var item in Model)
                {
                    sirano++;
                    <tr>
                        <td style="width:20px">@sirano</td>
                        <td class="text-center" style="width:20px">
                            <a href="#" onclick="getAdayBasvuruBilgileri('@item.TC'); getAdayIletisimBilgileri('@item.TC'); getAdayMYSSBilgileri('@item.TC'); getAdayTYSBilgileri('@item.TC'); return false;" style="cursor:pointer">@item.TC</a>
                        </td>
                        <td style="width:20px">@item.Ad</td>
                        <td style="width:20px">@item.Soyad</td>
                        <td style="width:20px">@item.MulakatYil</td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>
